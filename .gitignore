node_modules
.next
out
build
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.env*
!.env.example
next-env.d.ts
.vercel
*.tsbuildinfo

# Local data store (development only)
.data

# Camera originals at the repo root only. public/media must ship, so this is
# anchored with a leading slash; a bare "media" ignored that folder too and
# every image 404ed on the first deploy.
/media
